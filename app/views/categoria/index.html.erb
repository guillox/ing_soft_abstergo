<h1>Indice de Categorias</h1>

<ul>
<% categoria = Categoria.all%>
<% categoria.each do |categoria| %>
<li>
	<%= categoria.nombre %>
	<%= link_to "Eliminar", categoria, method: :delete,class: 'btn btn-danger btn-sm' %>
</li>
<% end %>
</ul>
