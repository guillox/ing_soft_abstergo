
<%= form_for(@auction) do |f| %>
  <% if @auction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auction.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @auction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Nombre %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :Descripción %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :Link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="field">
    <%= f.label :Categoria %><br>
    <select><option> LISTA DE CATEGORIAS </option><select>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
