<h1>Subastas</h1>

<%= render 'tabs' %>

<% if @lecCAt == 1 %>
	<table class="table table-striped">
		<thead>
			<tr>
				<th class="col-sm-2">Nombre</th>
				<th class="col-sm-4">Descripcion</th>
				<th class="col-sm-2">Categoria</th>
				<th class="col-sm-1">Inicio</th>
				<th class="col-sm-1">Subastador</th>
				<th class="col-sm-2">Acción</th>
			</tr>
		</thead>

		<tbody>
			<% Auction.all.each do |a| %>
				<% if a.active %>
					<tr>
						<td><%= a.name %></td>
						<td><%= truncate(a.description, length: 120, omission: '...') %></td>
						<td><%= a.category.name %></td>
						<td><%= a.category.created_at.strftime("%d %b. %Y") %></td>
						<td><%= a.user.username %></td>

						<td>
							<%= link_to 'Ver', auction_path(a), class: 'btn btn-warning' %>				
							<% if current_user %>
								<% if a.user_owner?(current_user) %>
									<% if  a.bids.empty? %>
										<%= link_to 'Editar', edit_auction_path(a), class: 'btn btn-primary' if can?(:update, a) %>
									<%end%>
								
									<%= link_to "Eliminar", a, method: :delete, data: { confirm:"¿Está seguro?" }, class: 'btn btn-danger' if can?(:destroy, a) %>	
								<% end %>
							<% end %>

						</td>
					</tr>
				<% end %>
			<% end %>  
		</tbody>
	</table>
<% else %>
	<% if @lecCAt == 2 %>

		<h1>Subastas por Categorias</h1>

		<div class="form-group">
			<%= label_tag "Categoría" %>
			<%= select_tag :category_id , options_from_collection_for_select(Category.all, :id, :name, 25), :include_blank => true , class: "form-control" %>
		</div>
		<%= submit_tag("Aceptar") %> 
	<% end %>
<% end %>
