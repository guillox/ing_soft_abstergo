<h1>Categorias</h1>

<%= render 'tabs' %>

<table class="table table-striped">
	<thead>
		<tr>
			<th class="col-sm-10">Nombre</th>
			<th class="col-sm-2">Acción</th>
		</tr>
	</thead>

	<tbody>
		<% @category.each do |category| %>
			<tr>
				<td><%= category.nombre %></td>
				<td>
					<%= link_to "Editar", edit_category_path(category), class: 'btn btn-info btn-xs' %>					

				  <%= link_to "Eliminar", category, method: :delete, data:{confirm:'¿Estás seguro?'}, class: 'btn btn-danger btn-xs' %>
				</td>
			</tr>
		<% end %>  
	</tbody>
</table>