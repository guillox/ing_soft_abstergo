<h1>Listing users</h1>


<table class="table table-striped">
	<thead>
		<tr>
			<th class="col-sm-8">Email</th>
			<th class="col-sm-3">Acción</th>
		</tr>
	</thead>

	<tbody>
    <% @users.each do |user| %>
	<% if user.activo == true %>
		<tr>	
			<td><%= user.email %></td>
	       		<td>
			<%= link_to 'Ver Cuenta', user , class: 'btn btn-primary'%>
        		<%= link_to 'Editar Cuenta', edit_user_path(user) , class: 'btn btn-primary'%>
        		<%= link_to 'Cerrar Cuenta', user, method: :delete, data: { confirm: '¿Estas Seguro?' } , class: 'btn btn-danger' %>
			</td>
		</tr>
	<% end 	%>      
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User', new_user_path, class: 'btn btn-primary'%>
