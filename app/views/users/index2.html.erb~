<% if current_user.admin? %>
<h1>Listado de Usuarios In-Activos</h1>


	<table class="table table-striped">
		<thead>
			<tr>
				<th class="col-sm-9">Email</th>
				<th class="col-sm-2">Acción</th>
			</tr>
		</thead>

		<tbody>
	    <% @users.each do |user| %>
		<% if user.activo == false %>
			<tr>	
				<td><%= user.email %></td>
		       		<td>
				<%= link_to 'Ver Cuenta', user , class: 'btn btn-primary'%>
				<%= link_to 'Re-Activar Cuenta', user, method: :reActivate, data: { confirm: '¿Estas Seguro?' } , class: 'btn btn-danger' %>
				</td>
			</tr>
	    <% end %>
	<% end %>
	  </tbody>
	</table>

	<br>


<% else %>
	<h3 style="color:red">ATENCIÓN: No tiene los permisos para acceder a esta pagina</h3>
<% end %>
